# captured frames / proof images (regenerated by the scripts)
*.png
*.jpg
*.jpeg
*.ppm

# local machine restore state (root-owned; created by install.sh --backup)
backup-*/

# python bytecode cache
__pycache__/
*.pyc
